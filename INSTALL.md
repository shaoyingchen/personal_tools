# Prompt Manager 安装教程

## 📋 项目简介

**Prompt Manager** 是一个功能完整的本地提示词管理系统，支持��
- ✅ AI 优化提示词
- ✅ 版本控制和管理
- ✅ 多种 AI 服务提供商
- ✅ 暗黑/明亮主题切换
- ✅ 提示词导入导出
- ✅ 搜索和标签管理

## 🚀 快速安装

### 方式一：Docker 部署（推荐）

1. **下载项目**
   ```bash
   # 下载发布包
   wget [你的下载链接] prompt-manager.tar.gz

   # 解压
   tar -xzf prompt-manager.tar.gz
   cd prompt-manager
   ```

2. **启动服务**
   ```bash
   # 启动服务
   docker compose up -d

   # 查看运行状态
   docker compose ps
   ```

3. **访问应用**
   - 打开浏览器访问：`http://localhost:3501`
   - 首次使用需要配置 AI 服务

### 方式二：手动部署

1. **环境要求**
   - Python 3.11+
   - SQLite 3

2. **安装依赖**
   ```bash
   # 安装 Python 依赖
   pip install -r requirements.txt
   ```

3. **启动应用**
   ```bash
   # 启动 Flask 应用
   python app.py
   ```

4. **访问应用**
   - 打开浏览器访问：`http://localhost:3501`

## ⚙️ AI 服务配置

### OpenAI 兼容服务

1. 访问 `http://localhost:3501/ai-configs`
2. 点击"新建 AI 配置"
3. 填写配置信息：
   ```
   名称: OpenAI API
   API 地址: https://api.openai.com/v1/chat/completions
   模型: gpt-4
   API 密钥: sk-xxxxxxxx
   ```

### 其他支持的服务

- **Claude API**: Anthropic Claude
- **本地模型**: Ollama, LocalAI
- **国内服务**: 智谱、文心一言等

## 📱 使用指南

### 基本操作

1. **创建提示词**
   - 点击"新建提示词"
   - 填写标题和内容
   - 添加标签和备注

2. **AI 优化**
   - 选择提示词
   - 点击"AI 优化"
   - 选择 AI 配置并提交
   - 等待优化完成
   - 查看结果并应用

3. **版本管理**
   - 查看历史版本
   - 对比不同版本
   - 回滚到之前版本

### 高级功能

- **批量导入**: 支持 JSON/Markdown 格式
- **搜索过滤**: 按名称、标签、内容搜索
- **主题切换**: 支持明亮/暗黑模式
- **导出分享**: 支持多种格式导出

## 🔧 故障排除

### 常见问题

1. **服务无法启动**
   ```bash
   # 检查端口占用
   netstat -tulpn | grep 3501

   # 修改端口（编辑 docker-compose.yml）
   ports:
     - "3502:3501"  # 改为其他端口
   ```

2. **AI 优化失败**
   - 检查 API 密钥是否正确
   - 确认网络连接正常
   - 查看 Docker 日志：
     ```bash
     docker logs prompt-manager-prompt-manager-1
     ```

3. **数据库错误**
   - 重置数据库：
     ```bash
     docker compose down
     docker volume rm prompt-manage_data
     docker compose up -d
     ```

### 数据备份

```bash
# 备份数据库
docker cp prompt-manage-prompt-manager-1:/app/data/data.sqlite3 ./backup/

# 恢复数据库
docker cp ./backup/data.sqlite3 prompt-manage-prompt-manager-1:/app/data/
docker restart prompt-manage-prompt-manager-1
```

## 🛠️ 开发者指南

### 项目结构

```
prompt-manager/
├── app.py              # Flask 主应用
├── requirements.txt     # Python 依赖
├── docker-compose.yml  # Docker 配置
├── Dockerfile         # 容器镜像
├── static/            # 前端资源
│   ├── css/
│   └── js/
├── templates/         # HTML 模板
└── data/             # 数据目录
```

### 本地开发

```bash
# 安装开发依赖
pip install -r requirements.txt

# 启动开发服务器
python app.py

# 或者使用 Docker
docker compose up --build
```

## 📄 许可证

本项目采用 MIT 许可证，详见 LICENSE 文件。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 支持

如遇问题，请：
1. 查看本文档的故障排除部分
2. 检查 GitHub Issues
3. 提交新的 Issue

---

**版本**: v1.2.0
**更新日期**: 2025-11-09
**兼容性**: Docker / Python 3.11+